// Ruta base de la API (ajústala según tu entorno).
const apiUrl = 'http://localhost/proyecto/public';  // O cambia esta URL si es necesario.

// Puedes agregar más configuraciones globales aquí si es necesario (por ejemplo, parámetros de autenticación o configuraciones de headers)
