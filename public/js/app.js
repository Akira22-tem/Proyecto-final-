// Configuración de la API, se puede ajustar según el entorno

// Si es necesario, se pueden agregar otras configuraciones globales o eventos aquí.
document.addEventListener('DOMContentLoaded', () => {
  // No hace falta cargar datos de autores o libros aquí, eso se maneja en los archivos específicos.
  // Si se requiere lógica global, se puede añadir aquí.
});
